apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose --file docker-compose-app.yml convert
    kompose.version: 1.26.1 (a9d05d509)
  labels:
    app: hummerrisk
  name: hummerrisk-web
spec:
  ports:
    - name: "web-hummerrisk"
      port: 80
      targetPort: 8088
  selector:
    app: hummerrisk